#include <stdio.h>

/**
 * main - prints numbers from zero to nine
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++i)
		putchar(i + '0');
	putchar('\n');
	return (0);
}

